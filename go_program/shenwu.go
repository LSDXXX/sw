package main

import (
	"shenwu"
)

type test struct {
	X      int
	Y      int
	Width  int
	Height int
}

func main() {
	//获取截图
	/*dir_path := "E:\\train_co\\test"
	for i := 0; i < 50; i++ {
		file_name := dir_path + "\\" + strconv.Itoa(i) + ".bmp"
		robotgo.Sleep(5
		shenwu.GetPositionImg(file_name)

	*/
	shenwu.BuyPets("11.bmp")
}
